# Neural Engram dependencies
# Install with: pip install -r requirements_neural.txt
#
# For CUDA 12.1 (recommended for RTX 3070 Ti):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements_neural.txt
#
# For CPU-only:
#   pip install torch
#   pip install -r requirements_neural.txt

torch>=2.2.0
transformers>=4.38.0
accelerate>=0.27.0   # optional: enables mixed precision and multi-GPU
datasets>=2.18.0     # already installed for benchmark suites
